# Development-only SQL Server password used by Docker Compose.
MSSQL_SA_PASSWORD=DynamicHr!Passw0rd

# Development-only PostgreSQL password used by the PostgreSQL Compose profile.
POSTGRES_PASSWORD=postgres
